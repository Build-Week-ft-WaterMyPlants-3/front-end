import React, { useState } from 'react';

const initialValue = {
	id: '',
	nickname: '',
	species: '',
	h2oFrequency: '',
	image: '',
};
const AuthotizedUser = (props) => {
	const [plant, setplant] = useState(initialValue);
	return <div></div>;
};

export default AuthotizedUser;
